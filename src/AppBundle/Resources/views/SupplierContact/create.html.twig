{% extends "::base.html.twig" %}
{% form_theme form 'AppBundle::Form/fields.html.twig' %}

{% block title %}{{ parent() }} :: Supplier contact {{ submit_label|default('Add') }}{% endblock %}

{% block body %}

    <h2 class="text-center">Supplier contact details:</h2>
    <h4 class="text-info text-center">{{ supplier.name }}</h4>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <div class="form-group text-center">
        <input type="submit" value="{{ submit_label|default('Add') }}" class="btn btn-lg btn-info" />
    </div>
    {{ form_end(form) }}
{% endblock %}
{% block controls %}

{% endblock %}